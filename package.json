{
  "name": "bp_reminder",
  "version": "1.0.0",
  "description": "",
  "main": "lib/index.js",
  "scripts": {
    "prepare": "npm run build",
    "build": "npm run clean && tsc -p .",
    "dev": "ts-node ./index.ts",
    "clean": "rm -rf ./lib",
    "start": "functions-framework --source=lib/ --target=bpReminder --signature-type=cloudevent",
    "main": "node ./lib/main.js",
    "test": "npm run build && jest",
    "watch": "tsc -w"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^16.10.2",
    "@types/request": "^2.48.7",
    "tslint": "^6.1.3",
    "typescript": "^4.4.3"
  },
  "dependencies": {
    "@google-cloud/functions-framework": "1.7.1",
    "@notionhq/client": "^0.4.4",
    "request": "^2.88.2"
  }
}
